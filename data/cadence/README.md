# Cadence Registry

This folder tracks Sacred Studios sound recording metadata for Cadence and ISRC review.

The registry is intentionally conservative. It stores registrant information, the Sacred Studios ISRC prefix, the next available sequence, and recording review records, but it does not auto-assign ISRC codes.

Fred must explicitly approve any real ISRC assignment before a code is written to a production manifest or registry record.
